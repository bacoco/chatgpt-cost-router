# T33 live mesh registration and automatic cross-node dispatch — 2026-09-10

Status: `PASS`

## What was verified

- A Mac Studio ran the T33 mesh control plane behind private Tailscale Serve.
- A separate MacBook worker host ran the heartbeat agent and registered node alias `macbook-pro-de-loic`.
- The registration advertised only worker alias `openai-B`, provider/cost/priority/readiness/auth state and non-secret budget observations.
- The control plane listed the node as live with a fresh heartbeat and exposed namespaced worker `macbook-pro-de-loic/openai-B`.
- Quota observations remained unknown (`null`) rather than being invented.
- A mesh request using `--worker auto` dynamically resolved the registered node and forwarded the prompt to its validated T32 remote-worker endpoint.

## Live result

```text
MESH_OK
worker_alias=openai-B
paid_api_used=NO
```

Observed telemetry:

- mesh worker: `macbook-pro-de-loic/openai-B`
- node location: `remote`
- provider: `openai`
- model: `gpt-6-astra`
- reported tokens: `4,612`
- elapsed: `5.888 s`
- exit code: `0`
- sandbox: `read-only`
- ephemeral: `true`
- paid-API environment removed: `true`

## Boundary

This proves automatic heartbeat registration/discovery and automatic cross-machine dispatch through the mesh. It does not yet prove load balancing among two simultaneously live worker-bearing nodes; the live mesh contained one eligible worker node in this smoke. The caller used the owner's Tailscale identity; a separately shared external-user identity remains a distinct future validation.

No paid OpenAI API was used.
