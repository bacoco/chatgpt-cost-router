# T33A — mesh registration/routing code verification

Status: `PASS_LOCAL_CODE_ONLY`

Date: 2026-09-10

Scope: automatic worker-node registration, TTL discovery, namespaced worker inventory and cross-node forwarding logic. No live model call was made for this verification.

Observed verification:

- 7/7 isolated `tests/test_mesh.py` tests PASS;
- Python compilation PASS for `cost_router/mesh.py`, `scripts/mesh_control_server.py`, `scripts/mesh_node_agent.py`, and `scripts/mesh_client.py`;
- Bash syntax PASS for mesh control/node start/stop helpers;
- registration advertisement excludes `CODEX_HOME` and local paths;
- runtime owner identity is stored only as a SHA-256 binding, not the login string;
- stale nodes expire by TTL;
- ambiguous duplicate worker aliases fail closed unless addressed as `node/worker`;
- only HTTPS `.ts.net` node endpoints are accepted;
- paid-API worker cost class is rejected;
- no Codex/model invocation occurred during this T33A verification.

Boundary: this receipt does not claim a live multi-node mesh. T33 requires one worker node to register through Tailscale Serve to a control plane on another machine and one bounded dispatch through the control plane.
