{
  "version": 1,
  "job_id": "job-t38-enable-high-risk-nonroot-20260911",
  "host": "macbook",
  "action": "exec_write",
  "args": {
    "argv": ["python3", "-c", "import json,os; from pathlib import Path; p=Path.home()/'.config/chatgpt-cost-router/fleet-operator.json'; d=json.loads(p.read_text()); [(v.__setitem__('allow_unrestricted_exec', True)) for v in d['hosts'].values()]; t=p.with_suffix('.tmp'); t.write_text(json.dumps(d,indent=2,sort_keys=True)+'\\n'); os.chmod(t,0o600); t.replace(p); os.chmod(p,0o600); print('enabled_high_risk_for_nonroot_aliases=',sorted(d['hosts']))"],
    "timeout_seconds": 30
  },
  "expires_at_unix": 1789120000
}
